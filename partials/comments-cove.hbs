<span class="is-hidden" id="cove-count"></span>
<div id="cove" class="comments__panel text-center w-100 text-acc"></div>

{{#if @member}}
  <div>{{@member.email}} · {{#link href="javascript:" data-members-signout=""}}{{t "Log out"}}{{/link}}</div>
{{else}}
  <p id="form-input-label" class="m-b fw-500 text-acc text-center text-11">{{t "Join the conversation"}}</p>
  {{> subscribe-form cove=true form_type="signin" button_text=(t 'Send login link')}}
{{/if}}

<script>
  const Cove = {
    publication: "{{#if @custom.comment_plugin_id}}{{@custom.comment_plugin_id}}{{/if}}",
    contentId: "{{id}}",
    memberId: "{{@member.uuid}}",
    memberEmail: "{{@member.email}}",
    memberName: "{{@member.firstname}}"
  }
</script>

<script src="https://cdn.cove.chat/comments.js" {{#if @custom.comment_plugin_id}}data-cove-id="{{@custom.comment_plugin_id}}"{{/if}} async defer></script>
